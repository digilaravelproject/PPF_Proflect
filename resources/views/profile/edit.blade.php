<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>My profile · Proflect</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="dashboard-body"><aside class="sidebar"><div class="sidebar__brand"><x-brand light/></div><nav class="sidebar__nav"><a href="{{ route('dashboard') }}"><span>⌂</span>Overview</a><a class="active" href="{{ route('profile.edit') }}"><span>♙</span>My profile</a><a href="{{ route('subscription.index') }}"><span>◇</span>Protection plans</a></nav><form method="POST" action="{{ route('logout') }}">@csrf<button class="sidebar__logout"><span>↪</span>Sign out</button></form></aside><main class="dashboard-main"><header class="topbar"><div><p class="topbar__kicker">CUSTOMER PORTAL</p><h1>My Profile</h1></div></header><div class="dashboard-content">@if(session('status'))<div class="alert alert--success">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert admin-error">{{ $errors->first() }}</div>@endif<section class="form-page-heading"><span class="eyebrow">ACCOUNT SETTINGS</span><h2>Personal details</h2><p>Keep your account information up to date.</p></section><form class="admin-form panel" method="POST" action="{{ route('profile.update') }}">@csrf @method('PUT')<div class="field-grid"><div class="field"><label>Full name</label><div class="field__control"><input name="name" value="{{ old('name',$user->name) }}" required></div></div><div class="field"><label>Email address</label><div class="field__control"><input name="email" type="email" value="{{ old('email',$user->email) }}" required></div></div></div><div class="field"><label>Phone number</label><div class="field__control"><input name="phone" value="{{ old('phone',$user->phone) }}" placeholder="Optional"></div></div><hr><h3>Change password <small>Leave blank to keep current password</small></h3><div class="field-grid"><div class="field"><label>Current password</label><div class="field__control"><input type="password" name="current_password"></div></div><div class="field"><label>New password</label><div class="field__control"><input type="password" name="password"></div></div></div><div class="field"><label>Confirm password</label><div class="field__control"><input type="password" name="password_confirmation"></div></div><button class="button button--dark">Save changes <span>→</span></button></form></div></main></body></html>
