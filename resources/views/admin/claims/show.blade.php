<x-admin-layout title="Claim Review">
    <section class="form-page-heading"><a href="{{ route('admin.claims.index') }}">← Back to claims</a><h2>Claim {{ $claim->claim_number }}</h2><p>Submitted {{ $claim->created_at->format('d M Y, h:i A') }}</p></section>
    <div class="admin-claim-grid"><section class="panel"><div class="claim-meta-grid"><div><span>CUSTOMER</span><b>{{ $claim->user->name }}</b><small>{{ $claim->user->email }} · {{ $claim->user->phone ?: 'No phone' }}</small></div><div><span>WARRANTY CODE</span><b class="code-value">{{ $claim->warrantyCode?->code ?: '—' }}</b></div><div><span>PLAN</span><b>{{ $claim->subscription->plan->name }}</b><small>Valid until {{ $claim->subscription->ends_at->format('d M Y') }}</small></div><div class="vehicle-meta"><span>VEHICLE</span><b>{{ trim($claim->vehicle_make.' '.$claim->vehicle_model) ?: 'Not provided' }}</b><small>{{ $claim->registration_number ?: '—' }}{{ $claim->vehicle_year ? ' · '.$claim->vehicle_year : '' }}</small></div><div><span>STATUS</span><b class="claim-status claim-status--{{ $claim->status }}">{{ ucfirst($claim->status) }}</b></div>@if($claim->booking_date)<div><span>BOOKING DATE</span><b>{{ $claim->booking_date->format('d M Y') }}</b></div>@endif</div>
        <div class="admin-panel-review"><x-vehicle-diagram :selected="$claim->panels"/><div><span class="eyebrow">CUSTOMER SELECTED PANELS</span><h3>{{ count($claim->panels) }} damaged {{ Str::plural('panel', count($claim->panels)) }}</h3><ul>@foreach($claim->panels as $panel)@php($detail = collect($claim->panel_details ?? [])->firstWhere('key', $panel))<li>{{ $detail['name'] ?? $panels[$panel] ?? $panel }}@if(isset($detail['min_sqm'], $detail['max_sqm'])) (min {{ $detail['min_sqm'] }} m², max {{ $detail['max_sqm'] }} m²)@endif</li>@endforeach</ul></div></div>@if($claim->description)<div class="claim-description"><span class="eyebrow">CUSTOMER NOTES</span><p>{{ $claim->description }}</p></div>@endif<span class="eyebrow">DAMAGE PHOTOS</span><div class="claim-photo-grid admin-photos">@foreach($claim->photos as $index=>$photo)<a href="{{ route('admin.claims.photo',[$claim,$index]) }}" target="_blank"><img src="{{ route('admin.claims.photo',[$claim,$index]) }}" alt="Damage evidence {{ $index+1 }}"></a>@endforeach</div></section>
        <aside class="panel decision-card"><span class="eyebrow">REVIEW DECISION</span><h3>Approve or decline</h3><form method="POST" action="{{ route('admin.claims.update',$claim) }}">@csrf @method('PUT')<div class="field"><label>Booking date <small>Required for approval</small></label><div class="field__control"><input type="date" name="booking_date" min="{{ now()->toDateString() }}" value="{{ old('booking_date',$claim->booking_date?->toDateString()) }}"></div>@error('booking_date')<p class="field__error">{{ $message }}</p>@enderror</div><div class="field"><label>Admin notes / decline reason</label><textarea name="admin_notes" rows="7" placeholder="A reason is required when declining a claim.">{{ old('admin_notes',$claim->admin_notes) }}</textarea>@error('admin_notes')<p class="field__error">{{ $message }}</p>@enderror</div><div class="decision-actions"><button class="button button--approve" name="status" value="approved">✓ Approve</button><button class="button button--decline" name="status" value="disapproved">× Decline</button></div></form><form method="POST" action="{{ route('admin.claims.destroy',$claim) }}" onsubmit="return confirm('Permanently delete this claim?')">@csrf @method('DELETE')<button class="danger-link">× Delete claim</button></form></aside>
    </div>
</x-admin-layout>
