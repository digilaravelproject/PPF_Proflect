<x-admin-layout title="Claims"><section class="welcome-row"><div><h2>Customer claims</h2><p>Review submitted damage evidence and manage claim decisions.</p></div><a href="{{ route('admin.claims.report',request()->query()) }}" class="button button--dark">Download PDF <span>⇩</span></a></section><form class="admin-filters" method="GET"><div class="field"><label>Search</label><div class="field__control"><input name="search" value="{{ request('search') }}" placeholder="Claim ID, customer or email"></div></div><div class="field"><label>Status</label><div class="field__control"><select name="status"><option value="">All statuses</option>@foreach(['pending','approved','disapproved'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div></div><div class="field"><label>From</label><div class="field__control"><input type="date" name="from" value="{{ request('from') }}"></div></div><div class="field"><label>To</label><div class="field__control"><input type="date" name="to" value="{{ request('to') }}"></div></div><button class="button button--dark">Filter</button><a href="{{ route('admin.claims.index') }}">Clear</a></form><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Claim</th><th>Customer</th><th>Vehicle</th><th>Plan</th><th>Panels</th><th>Submitted</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($claims as $claim)<tr><td><b>{{ $claim->claim_number }}</b></td><td><span><b>{{ $claim->user->name }}</b><small>{{ $claim->user->email }}</small></span></td><td><span><b>{{ trim($claim->vehicle_make.' '.$claim->vehicle_model) ?: '—' }}</b><small>{{ $claim->registration_number ?: '—' }}</small></span></td><td>{{ $claim->subscription->plan->name }}</td><td>{{ count($claim->panels) }}</td><td>{{ $claim->created_at->format('d M Y') }}</td><td><span class="claim-status claim-status--{{ $claim->status }}">{{ ucfirst($claim->status) }}</span></td><td><div class="table-actions"><a class="icon-action" href="{{ route('admin.claims.show',$claim) }}" title="Review claim" aria-label="Review claim">◉</a><form method="POST" action="{{ route('admin.claims.destroy',$claim) }}" onsubmit="return confirm('Delete this claim and its uploaded photos?')">@csrf @method('DELETE')<button class="icon-action icon-action--danger" title="Delete claim" aria-label="Delete claim">×</button></form></div></td></tr>@empty<tr><td colspan="8">No claims match these filters.</td></tr>@endforelse</tbody></table></div>@if($claims->hasPages())<div class="admin-pagination">{{ $claims->links() }}</div>@endif</x-admin-layout>
