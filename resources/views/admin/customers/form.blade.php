<x-admin-layout :title="$customer->exists ? 'Edit Customer' : 'Create Customer'"><section class="form-page-heading"><a href="{{ route('admin.customers.index') }}">← Back to customers</a><h2>{{ $customer->exists ? 'Edit customer' : 'Create a customer' }}</h2><p>{{ $customer->exists ? 'Update profile details or set a new password.' : 'Add a customer who can sign in to the customer portal.' }}</p></section><form class="admin-form panel" method="POST" action="{{ $customer->exists ? route('admin.customers.update',$customer) : route('admin.customers.store') }}">@csrf @if($customer->exists)@method('PUT')@endif<div class="field-grid"><div class="field"><label>Full name</label><div class="field__control"><input name="name" value="{{ old('name',$customer->name) }}" required></div></div><div class="field"><label>Email address</label><div class="field__control"><input type="email" name="email" value="{{ old('email',$customer->email) }}" required></div></div><div class="field"><label>Phone number</label><div class="field__control"><input type="tel" name="phone" value="{{ old('phone',$customer->phone) }}" required></div></div><div></div><div class="field"><label>Password {{ $customer->exists ? '(leave blank to keep current)' : '' }}</label><div class="field__control"><input type="password" name="password" {{ $customer->exists ? '' : 'required' }}></div></div><div class="field"><label>Confirm password</label><div class="field__control"><input type="password" name="password_confirmation" {{ $customer->exists ? '' : 'required' }}></div></div></div><label class="check-row admin-active"><input type="checkbox" name="onboarding_completed" value="1" @checked(old('onboarding_completed',$customer->onboarding_completed_at !== null))><span>Onboarding completed (customer can access dashboard)</span></label><button class="button button--dark">{{ $customer->exists ? 'Save changes' : 'Create customer' }} <span>→</span></button></form></x-admin-layout>
