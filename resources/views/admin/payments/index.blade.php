<x-admin-layout title="Payments"><section class="welcome-row"><div><h2>Payment history</h2><p>View Razorpay payments and free subscription activations across all customers.</p></div><a href="{{ route('admin.reports.index') }}" class="button button--dark">Open reports <span>→</span></a></section><form class="admin-filters admin-filters--payments" method="GET"><div class="field"><label>Search</label><div class="field__control"><input name="search" value="{{ request('search') }}" placeholder="Customer, order or payment ID"></div></div><div class="field"><label>Status</label><div class="field__control"><select name="status"><option value="">All statuses</option>@foreach(['created','paid','verification_failed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst(str_replace('_',' ',$status)) }}</option>@endforeach</select></div></div><div class="field"><label>Type</label><div class="field__control"><select name="gateway"><option value="">All types</option><option value="razorpay" @selected(request('gateway')==='razorpay')>Razorpay</option><option value="free" @selected(request('gateway')==='free')>Free plan</option></select></div></div><div class="field"><label>From</label><div class="field__control"><input type="date" name="from" value="{{ request('from') }}"></div></div><div class="field"><label>To</label><div class="field__control"><input type="date" name="to" value="{{ request('to') }}"></div></div><button class="button button--dark">Filter</button><a href="{{ route('admin.payments.index') }}">Clear</a></form><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Payment</th><th>Customer</th><th>Plan</th><th>Type</th><th>Amount</th><th>Date</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td><b>{{ $payment->gateway === 'free' ? 'FREE-'.str_pad($payment->id,6,'0',STR_PAD_LEFT) : ($payment->gateway_payment_id ?: $payment->gateway_order_id) }}</b></td><td><span><b>{{ $payment->user->name }}</b><small>{{ $payment->user->email }}</small></span></td><td>{{ $payment->plan->name }}</td><td>{{ $payment->gateway === 'free' ? 'Free plan' : ucfirst($payment->gateway) }}</td><td>{{ $payment->formatted_amount }}</td><td>{{ ($payment->paid_at ?? $payment->created_at)->format('d M Y') }}</td><td><span class="claim-status claim-status--{{ $payment->status === 'paid' ? 'approved' : ($payment->status === 'verification_failed' ? 'disapproved' : 'pending') }}">{{ ucfirst(str_replace('_',' ',$payment->status)) }}</span></td><td><a class="icon-action" href="{{ route('admin.payments.show',$payment) }}" title="View payment" aria-label="View payment"><x-eye-icon/></a></td></tr>@empty<tr><td colspan="8">No payments match these filters.</td></tr>@endforelse</tbody></table></div>@if($payments->hasPages())<div class="admin-pagination">{{ $payments->links() }}</div>@endif</x-admin-layout>
