<x-admin-guest-layout><x-slot:title>Reset admin password</x-slot:title><div class="form-heading"><span class="eyebrow">ADMIN ACCOUNT RECOVERY</span><h2>Choose a new password</h2></div><form method="POST" action="{{ route('admin.password.update') }}" class="auth-form">@csrf<input type="hidden" name="token" value="{{ $token }}"><div class="field"><label for="email">Email address</label><div class="field__control"><input id="email" type="email" name="email" value="{{ old('email', $email) }}" required></div>@error('email')<p class="field__error">{{ $message }}</p>@enderror</div><div class="field"><label for="password">New password</label><div class="field__control"><input id="password" type="password" name="password" required autocomplete="new-password"></div>@error('password')<p class="field__error">{{ $message }}</p>@enderror</div><div class="field"><label for="password_confirmation">Confirm password</label><div class="field__control"><input id="password_confirmation" type="password" name="password_confirmation" required autocomplete="new-password"></div></div><button class="button button--primary">Reset password →</button></form></x-admin-guest-layout>
