<x-admin-guest-layout><x-slot:title>Admin password recovery</x-slot:title><a class="back-link" href="{{ route('admin.login') }}">← Admin sign in</a><div class="form-heading"><span class="eyebrow">ADMIN ACCOUNT RECOVERY</span><h2>Forgot your password?</h2><p>Enter your admin email and we’ll send a secure reset link.</p></div>@if(session('status'))<div class="alert alert--success">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('admin.password.email') }}" class="auth-form">@csrf<div class="field"><label for="email">Email address</label><div class="field__control"><input id="email" type="email" name="email" value="{{ old('email') }}" required autofocus></div>@error('email')<p class="field__error">{{ $message }}</p>@enderror</div><button class="button button--primary">Send reset link →</button></form></x-admin-guest-layout>
