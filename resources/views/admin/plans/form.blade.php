<x-admin-layout :title="$plan->exists ? 'Edit Plan' : 'Create Plan'"><section class="form-page-heading"><a href="{{ route('admin.plans.index') }}">← Back to plans</a><h2>{{ $plan->exists?'Edit protection plan':'Create a protection plan' }}</h2><p>Use a price of 0 and a day duration for a free plan. Changes to active plans appear on the customer subscription screen.</p></section><form class="admin-form panel" method="POST" action="{{ $plan->exists?route('admin.plans.update',$plan):route('admin.plans.store') }}">@csrf @if($plan->exists)@method('PUT')@endif<div class="field-grid"><div class="field"><label>Plan name</label><div class="field__control"><input name="name" value="{{ old('name',$plan->name) }}" required></div></div><div class="field"><label>URL slug</label><div class="field__control"><input name="slug" value="{{ old('slug',$plan->slug) }}" placeholder="Generated from name"></div></div><div class="field"><label>Price (₹)</label><div class="field__control"><input type="number" min="0" step="0.01" name="price_rupees" value="{{ old('price_rupees',$plan->exists?$plan->price/100:'') }}" required></div></div><div class="field"><label>Duration (years)</label><div class="field__control"><input type="number" min="0" name="duration_years" value="{{ old('duration_years',$plan->exists?$plan->duration_years:1) }}" required></div></div><div class="field"><label>Duration (days) <small>Overrides years when entered</small></label><div class="field__control"><input type="number" min="1" name="duration_days" value="{{ old('duration_days',$plan->duration_days) }}" placeholder="e.g. 15 for a free trial"></div></div><div class="field"><label>Coverage (m²)</label><div class="field__control"><input type="number" step="0.01" name="coverage_sqm" value="{{ old('coverage_sqm',$plan->coverage_sqm ?: 5) }}" required></div></div><div class="field"><label>Card style</label><div class="field__control"><select name="accent"><option value="silver" @selected(old('accent',$plan->accent)==='silver')>Silver</option><option value="gold" @selected(old('accent',$plan->accent)==='gold')>Gold</option><option value="black" @selected(old('accent',$plan->accent)==='black')>Black</option></select></div></div></div><div class="field"><label>Description</label><textarea name="description" rows="3">{{ old('description',$plan->description) }}</textarea></div><div class="field"><label>Features <small>One per line</small></label><textarea name="features_text" rows="6">{{ old('features_text',implode("\n",$plan->features ?? [])) }}</textarea></div><div class="field-grid"><div class="field"><label>Display order</label><div class="field__control"><input type="number" name="sort_order" value="{{ old('sort_order',$plan->sort_order ?? 0) }}" required></div></div><label class="check-row admin-active"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$plan->exists?$plan->is_active:true))><span>Active and visible to customers</span></label></div><button class="button button--dark">{{ $plan->exists?'Save changes':'Create plan' }} <span>→</span></button></form></x-admin-layout>
