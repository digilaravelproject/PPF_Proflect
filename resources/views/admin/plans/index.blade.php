<x-admin-layout title="Manage Plans"><section class="welcome-row"><div><h2>Protection plans</h2><p>Plans shown here are immediately reflected in the customer offer screen.</p></div><a href="{{ route('admin.plans.create') }}" class="button button--dark">Create plan <span>+</span></a></section><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Plan</th><th>Price</th><th>Term</th><th>Coverage</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($plans as $plan)<tr><td><span class="mini-plan mini-plan--{{ $plan->accent }}">{{ strtoupper(substr($plan->name,0,1)) }}</span><b>{{ $plan->name }}</b></td><td>{{ $plan->formatted_price }}</td><td>{{ $plan->duration_label }}</td><td>{{ $plan->coverage_sqm }} m²</td><td><span class="table-status {{ $plan->is_active?'active':'inactive' }}">{{ $plan->is_active?'Active':'Inactive' }}</span></td><td><div class="table-actions"><a class="icon-action" href="{{ route('admin.plans.edit',$plan) }}" title="Edit plan" aria-label="Edit plan">✎</a><form method="POST" action="{{ route('admin.plans.destroy',$plan) }}" onsubmit="return confirm('Delete this plan?')">@csrf @method('DELETE')<button class="icon-action icon-action--danger" title="Delete plan" aria-label="Delete plan">×</button></form></div></td></tr>@empty<tr><td colspan="6">No plans created.</td></tr>@endforelse</tbody></table></div></x-admin-layout>
