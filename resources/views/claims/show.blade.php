<x-customer-layout title="Claim Details"><section class="claim-result panel"><div class="success-check">✓</div><span class="eyebrow">{{ $claim->status === 'pending' ? 'CLAIM SUBMITTED' : 'CLAIM UPDATED' }}</span><h2>{{ $claim->status === 'pending' ? 'Claim Submitted Successfully' : 'Claim '.ucfirst($claim->status) }}</h2><p>Claim ID: <b>{{ $claim->claim_number }}</b></p><div class="claim-summary-grid"><div><span>VEHICLE</span><b>{{ trim($claim->vehicle_make.' '.$claim->vehicle_model) ?: 'Not provided' }}</b><small>{{ $claim->registration_number ?: '—' }}{{ $claim->vehicle_year ? ' · '.$claim->vehicle_year : '' }}</small></div><div><span>PLAN</span><b>{{ $claim->subscription->plan->name }}</b></div><div><span>PANELS</span><b>{{ count($claim->panels) }}</b></div><div><span>STATUS</span><b class="claim-status claim-status--{{ $claim->status }}">{{ ucfirst($claim->status) }}</b></div><div><span>SUBMITTED</span><b>{{ $claim->created_at->format('d M Y') }}</b></div></div><div class="claim-photo-grid">@foreach($claim->photos as $index=>$photo)<img src="{{ route('claims.photo',[$claim,$index]) }}" alt="Damage evidence {{ $index+1 }}">@endforeach</div>@if($claim->admin_notes)<div class="review-notice"><b>Admin notes</b><span>{{ $claim->admin_notes }}</span></div>@endif<p>We will review your claim and update you through this portal.</p><div class="claim-result-actions"><a href="{{ route('claims.index') }}" class="button button--dark">View claim history</a><a href="{{ route('dashboard') }}" class="text-link">Back to dashboard</a></div></section></x-customer-layout>
