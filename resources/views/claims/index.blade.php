<x-customer-layout title="My Claims"><section class="welcome-row"><div><h2>Claim history</h2><p>Track every claim and its current review status.</p></div><a href="{{ route('claims.create') }}" class="button button--dark">Make a claim <span>+</span></a></section><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Claim</th><th>Vehicle</th><th>Plan</th><th>Panels</th><th>Submitted</th><th>Status</th><th></th></tr></thead><tbody>@forelse($claims as $claim)<tr><td><b>{{ $claim->claim_number }}</b></td><td><span><b>{{ trim($claim->vehicle_make.' '.$claim->vehicle_model) ?: '—' }}</b><small>{{ $claim->registration_number ?: '—' }}</small></span></td><td>{{ $claim->subscription->plan->name }}</td><td>{{ count($claim->panels) }}</td><td>{{ $claim->created_at->format('d M Y') }}</td><td><span class="claim-status claim-status--{{ $claim->status }}">{{ ucfirst($claim->status) }}</span></td><td><a class="icon-action" href="{{ route('claims.show',$claim) }}" title="View claim" aria-label="View claim"><x-eye-icon/></a></td></tr>@empty<tr><td colspan="7">No claims submitted yet.</td></tr>@endforelse</tbody></table></div>@if($claims->hasPages())<div class="admin-pagination">{{ $claims->links() }}</div>@endif</x-customer-layout>
